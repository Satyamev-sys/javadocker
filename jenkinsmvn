pipeline {
    agent any

    stages {
        stage('Maven Installtion') {
            steps {
                sh 'sudo dnf install maven -y'
            }
        }
       stage('Create Package') {
            steps {
                sh 'mvn clean package'
             }
         } 
       stage('Release') {
            steps {
                archiveArtifacts artifacts: 'target/*.jar', followSymlinks: false
            } 
    }
}
}
